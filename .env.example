# PostgreSQL connection (asyncpg driver required)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/twogis

# 2GIS public reviews API key (from your parser.py)
TWOGIS_REVIEWS_API_KEY=

# 2GIS public web API key for the Catalog API (city/region list). Default works;
# override only if 2GIS rotates/blocks it.
TWOGIS_WEB_API_KEY=
# How long (seconds) to cache the Kazakhstan city catalog fetched from 2GIS.
CITIES_CACHE_TTL_SECONDS=86400

# Scraping behavior
MAX_CONCURRENT_BRANCHES=5
MAX_BRANCHES_PER_SEARCH=50
REQUEST_TIMEOUT_SECONDS=15
RATE_LIMIT_SLEEP_MIN=1.0
RATE_LIMIT_SLEEP_MAX=2.0

# Default city slug for 2GIS search URLs (e.g., almaty, astana, shymkent).
# The full list of Kazakhstan cities is available at GET /api/cities.
# Use city="all" in search requests to search across every Kazakhstan city.
DEFAULT_CITY_SLUG=almaty
